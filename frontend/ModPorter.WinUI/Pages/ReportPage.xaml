<Page
    x:Class="ModPorter.WinUI.Pages.ReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:ModPorter.WinUI.Models">

    <Page.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16,12"/>
        </Style>
    </Page.Resources>

    <Grid Padding="32,24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock Text="转换报告" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock x:Name="SubtitleText"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- 统计卡片 -->
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="自动改写" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock x:Name="InfoCount" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="警告" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock x:Name="WarnCount" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="待人工处理" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock x:Name="TodoCount" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="错误" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock x:Name="ErrorCount" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 过滤与操作 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <ComboBox x:Name="SeverityFilter" Header="级别过滤" Width="160"
                      SelectionChanged="SeverityFilter_SelectionChanged">
                <ComboBoxItem Content="全部" Tag="ALL" IsSelected="True"/>
                <ComboBoxItem Content="待人工 (TODO)" Tag="TODO"/>
                <ComboBoxItem Content="错误 (ERROR)" Tag="ERROR"/>
                <ComboBoxItem Content="警告 (WARN)" Tag="WARN"/>
                <ComboBoxItem Content="自动改写 (INFO)" Tag="INFO"/>
            </ComboBox>
            <Button Content="打开输出目录" VerticalAlignment="Bottom" Click="OpenOutput_Click"/>
            <Button Content="打开 Markdown 报告" VerticalAlignment="Bottom" Click="OpenMd_Click"/>
            <Button x:Name="TodoButton" Content="打开 TODO 清单" VerticalAlignment="Bottom" Click="OpenTodo_Click"/>
        </StackPanel>

        <!-- 条目列表 -->
        <ListView x:Name="EntryList" Grid.Row="3" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ReportEntry">
                    <Grid Padding="0,8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="64"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Severity}"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Text="{x:Bind Message}" TextWrapping="Wrap"/>
                            <TextBlock Text="{x:Bind Location}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
